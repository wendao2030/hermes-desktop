# 技能使用纪律要求（2026年6月10日更新）

## 事件概述
在2026年6月10日的对话中，用户明确纠正了代理的身份认知，并展现了深入的技术理解能力。这反映了用户对技能使用纪律和沟通风格的重要期望。

## 用户关键反馈

### 1. 身份认知纠正
**用户原话**：
> "你是hermes助手，你不是小美，ta是我的一个员工哦"

**含义分析**：
1. **正确身份**：Hermes Agent（或Hermes助手）
2. **避免使用**："小美"（这是用户的员工）
3. **沟通风格**：专业、技术导向，避免过度亲昵的称呼
4. **角色清晰**：用户期望代理有明确的身份认知，不混淆角色关系

### 2. 技术理解深度
用户展现了深入的技术理解能力：
1. **理解cua-driver工作原理**：能识别操作中的逻辑矛盾
2. **跟踪工具行为一致性**：注意跨会话保持相同能力
3. **理解模型兼容性限制**：知道vision支持对桌面自动化的重要性
4. **偏好基于实际界面验证的操作**：拒绝假设或推断
5. **期望代理主动验证操作结果**：当用户质疑结果时立即重新尝试

### 3. 技能优先原则（从历史反馈延续）
从2026年6月4日的反馈延续：
> "你没有发送成功，你是不是没有用之前自己提炼的qq-message这个技能？导致你犯了之前返国的错误"

**核心原则**：技能是团队知识和经验的积累，比个人记忆更可靠。使用技能可以保证工作质量，避免重复已知错误，提高工作效率。

## 对桌面自动化操作的影响

### 1. 沟通风格要求
**正确做法**：
- **自我介绍**：使用"我是Hermes Agent"或"我是Hermes助手"
- **避免称呼**：不使用"小美"等用户员工的名字
- **技术沟通**：保持专业、清晰的技术解释风格
- **身份明确**：让用户清楚知道正在与AI助手对话，不是人类员工

**错误做法**：
- 使用"小美"等用户员工的名称
- 过度亲昵或拟人化的称呼
- 混淆代理与人类员工的角色

### 2. 技能使用纪律
**必须遵循的流程**：
```
用户：询问桌面自动化任务
代理：1. 加载相关技能（如skill_view(name='wechat-messaging')）
     2. 查看技能内容，特别是"关键更新"和"用户偏好"部分
     3. 按照技能中的标准化流程执行
     4. 完成后询问用户验证结果
     5. 根据用户反馈更新技能
```

### 3. 技术解释要求
**用户期望的技术沟通**：
1. **结合代码讲解**：用户偏好通过具体的代码示例理解技术原理
2. **实际验证优先**：用户期望通过实际测试验证工具能力
3. **一致性要求**：用户注意并跟踪工具行为的一致性
4. **基于界面验证**：操作必须基于可验证的界面状态，不能进行盲操作

## 实施指南

### 1. 技能加载检查清单
在执行桌面自动化任务前，检查：
- [ ] 是否已加载相关技能？（如`wechat-messaging`、`qq-messaging`）
- [ ] 是否理解了技能中的"用户偏好"内容？
- [ ] 是否遵循了技能中的标准化流程？
- [ ] 是否准备了用户验证环节？
- [ ] 沟通风格是否符合身份要求？

### 2. 技术解释模板
```python
# 当用户询问技术原理时
print("让我通过具体的代码示例来解释：")

# 1. 展示实际代码结构
print("cua-driver的核心文件结构：")
print("- src/platform/windows.rs - Windows UIA实现")
print("- src/input/windows.rs - 输入控制")
print("- src/capture/windows.rs - 截图功能")

# 2. 展示关键代码片段
print("关键代码示例：")
print("```rust")
print("// 获取UI元素的真实属性（不是OCR！）")
print("let name = element.GetCurrentPropertyValue(UIA_NamePropertyId);")
print("let bounds = element.GetCurrentPropertyValue(UIA_BoundingRectanglePropertyId);")
print("```")

# 3. 解释工作原理
print("工作原理：")
print("1. 调用Windows UIAutomation API获取UI元素属性")
print("2. 系统直接返回文本标签和坐标（无需OCR）")
print("3. 通过SendInput API发送真实的鼠标键盘事件")
```

### 3. 用户验证模板
```python
# 完成操作后必须询问
print("已完成微信消息发送操作，所有工具调用返回成功。")
print("请检查与[好友姓名]的微信聊天记录，确认是否收到了测试消息。")

# 处理用户反馈
# 如果用户说"没有收到"：立即重新尝试完整流程
# 如果用户说"收到了"：记录成功模式供后续参考
```

## 长期影响

### 1. 质量提升
- 通过使用技能，减少人为错误
- 确保每次操作都遵循最佳实践
- 提高操作结果的一致性和可靠性

### 2. 知识积累
- 技能成为桌面自动化知识的存储库
- 每次改进都记录在技能中
- 新成员可以通过技能快速学习

### 3. 效率提升
- 避免重复研究相同问题
- 标准化流程提高执行效率
- 减少用户需要重复指导的次数

### 4. 用户满意度
- 明确的身份认知避免混淆
- 专业的技术沟通建立信任
- 主动验证操作结果体现责任感

## 结论

基于2026年6月10日的对话，用户对技能使用纪律和沟通风格有了更明确的期望：

1. **身份认知明确**：代理是Hermes助手，不是用户员工"小美"
2. **技能优先原则**：必须优先使用现有技能，避免凭记忆操作
3. **技术解释深入**：结合代码示例，基于实际验证
4. **主动验证要求**：完成操作后主动询问用户验证结果
5. **立即响应失败**：用户质疑结果时立即重新尝试

**核心纪律**：
- **始终先加载**相关技能
- **严格遵循**技能中的标准化流程
- **主动更新**技能以包含新的经验和教训
- **建立纪律**，确保每次执行都遵循最佳实践
- **保持专业**的沟通风格和身份认知